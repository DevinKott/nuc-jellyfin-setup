# NAS Mount and Docker Startup Service - Environment Configuration
#
# Copy this file to /etc/nas-mount-startup.env and fill in your values
# Command: sudo cp nas-mount-startup.env.example /etc/nas-mount-startup.env
# Then edit: sudo nano /etc/nas-mount-startup.env
#
# IMPORTANT: Set restrictive permissions on the actual config file:
# sudo chmod 600 /etc/nas-mount-startup.env

# ===== REQUIRED CONFIGURATION =====

# NAS Password (REQUIRED - no default)
# This is your password for accessing the NAS share
NAS_PASSWORD=your_nas_password_here

# Docker Compose Directory (REQUIRED - must be updated)
# Full path to the directory containing your compose.yml or docker-compose.yml file
DOCKER_COMPOSE_DIR=/path/to/your/docker/compose

# ===== OPTIONAL CONFIGURATION =====
# Uncomment and modify these if you need different values from the defaults

# NAS IP Address (default: 192.168.1.241)
#NAS_IP=192.168.1.241

# NAS Share Name (default: Media)
#NAS_SHARE=Media

# NAS Username (default: devin)
#NAS_USERNAME=devin

# Local Mount Point (default: /mnt/synology_nas/media)
#MOUNT_POINT=/mnt/synology_nas/media

# Log File Location (default: /var/log/nas-mount-startup.log)
#LOG_FILE=/var/log/nas-mount-startup.log

# Expected Folders (default: Movies,TV Shows,Music)
# Comma-separated list of folders to verify mount success
#EXPECTED_FOLDERS=Movies,TV Shows,Music,Photos

# ===== JELLYFIN CONFIGURATION =====
# These settings control the Docker Compose configuration for Jellyfin

# Jellyfin Docker Image (default: jellyfin/jellyfin)
#JELLYFIN_IMAGE=jellyfin/jellyfin

# Jellyfin Container Name (default: jellyfin)
#JELLYFIN_CONTAINER_NAME=jellyfin

# Jellyfin Configuration Directory (default: ~/jellyfin_config)
# This directory stores Jellyfin's configuration files and metadata
#JELLYFIN_CONFIG_DIR=~/jellyfin_config

# Jellyfin Cache Directory (default: ~/jellyfin_cache)
# This directory stores Jellyfin's cache data
#JELLYFIN_CACHE_DIR=~/jellyfin_cache

# Jellyfin HTTP Port (default: 8096)
# The port Jellyfin will be accessible on (uses host networking)
#JELLYFIN_PORT=8096

# Jellyfin Media Directory (default: uses MOUNT_POINT value)
# This is where Jellyfin will access media files from the NAS
# By default, it uses the same path as MOUNT_POINT
#JELLYFIN_MEDIA_DIR=/mnt/synology_nas/media

# ===== TINYMEDIAMANAGER CONFIGURATION =====
# These settings control the Docker Compose configuration for TinyMediaManager
# TinyMediaManager is a media management tool for organizing and managing your movie and TV show collections

# TinyMediaManager Docker Image (default: tinymediamanager/tinymediamanager:latest)
#TMM_IMAGE=tinymediamanager/tinymediamanager:latest

# TinyMediaManager Container Name (default: tmm)
#TMM_CONTAINER_NAME=tmm

# TinyMediaManager Data Directory (default: ~/tmm-data)
# This directory stores TinyMediaManager's configuration, cache, and metadata
#TMM_DATA_DIR=~/tmm-data

# TinyMediaManager HTTP Port (default: 4000)
# The port TinyMediaManager's web interface will be accessible on
#TMM_PORT=4000

# TinyMediaManager Timezone (default: America/Denver)
# Set the timezone for TinyMediaManager
#TMM_TZ=America/Denver

# TinyMediaManager TV Shows Directory (default: ${MOUNT_POINT}/TV Shows)
# Path to TV shows from the NAS
#TMM_TVSHOWS_DIR=/mnt/synology_nas/media/TV Shows

# TinyMediaManager Movies Directory (default: ${MOUNT_POINT}/Movies)
# Path to movies from the NAS
#TMM_MOVIES_DIR=/mnt/synology_nas/media/Movies
